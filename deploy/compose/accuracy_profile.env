export APP_NVINGEST_ENABLE_PAGED_DOC_SPLIT=True
export APP_NVINGEST_CHUNKSIZE=1024
export APP_NVINGEST_CHUNKOVERLAP=150
export ENABLE_RERANKER=True
export VECTOR_DB_TOPK=100
export APP_RETRIEVER_TOPK=10
# Agentic RAG: override retrieval breadth for the task-execute stage only
# (execute / execute_scope / verify_execute). Falls back to the values above
# when unset. Initial retrieval always uses VECTOR_DB_TOPK / APP_RETRIEVER_TOPK.
# export APP_AGENTIC_TASK_VECTOR_DB_TOPK=100
# export APP_AGENTIC_TASK_RETRIEVER_TOPK=10